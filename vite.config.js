import { defineConfig } from 'vite'

export default defineConfig({
  // Set base to your GitHub repo name for GitHub Pages deployment.
  // e.g. if your repo is github.com/username/Bored, set base to '/Bored/'
  base: '/Bored/',
})
